version: '3.8'

services:
  db:
    image: postgres:13
    container_name: odoo18-db
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: odoo
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - odoo-db-data:/var/lib/postgresql/data
    networks:
      - odoo-net

  odoo:
    build:
      context: .
      dockerfile: odooDockerfile
    container_name: odoo18
    depends_on:
      - db
    ports:
      - "8099:8069"
    environment:
      HOST: db
      USER: odoo
      PASSWORD: odoo
      DATABASE: odoo
    volumes:
      - odoo-data:/var/lib/odoo
    networks:
      - odoo-net

  django:
    container_name: defcon-ecommerce-api
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    depends_on:
      - db
    environment:
      - DJANGO_DB_NAME=odoo
      - DJANGO_DB_USER=odoo
      - DJANGO_DB_PASSWORD=odoo
      - DJANGO_DB_HOST=db
      - DJANGO_DB_PORT=5432
    ports:
      - "8000:8000"
    networks:
      - odoo-net

volumes:
  odoo-db-data:
  odoo-data:

networks:
  odoo-net:
    driver: bridge
